
# TODO: Use?

def chord(root, chord_template, pitches, score):
    return {'root': root,
            'chord_template': chord_template,
            'pitches': pitches,
            'score': score}
    
def chord_template(name, template):
    return {'name': name,
            'template': template}
    
    